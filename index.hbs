{{!< default}}

{{!-- Blog Title Section --}}
<section class="gh-blog-title-section">
    <div class="gh-container">
        <a class="gh-blog-title" href="{{@site.url}}">
            {{#if @site.logo}}
                <img src="{{@site.logo}}" alt="{{@site.title}}" />
            {{else}}
                {{@site.title}}
            {{/if}}
        </a>
    </div>
</section>

{{!-- Hero Section with Featured Posts Slider --}}
{{> "hero"}}

<div class="gh-page">
    <div class="gh-container">
        <div class="gh-latest-posts-header">
            <h2 class="gh-latest-posts-title">Latest Posts</h2>
        </div>

        <div id="posts" class="gh-postfeed">
        {{#foreach posts limit="6"}}
            {{> "card" page=../pagination.page }} {{!-- partials/card.hbs --}}
        {{/foreach}}
        </div>

        <div class="gh-more-posts-container">
            <a href="/archive/" class="gh-more-posts-button">More Posts →</a>
        </div>

        {{#if @site.members_enabled}}
            {{^if @member}}
            <div class="gh-subscribe-section" style="text-align: center; margin: 4rem 0;">
                <a class="gh-button" href="{{@site.signup_url}}" style="background: #800020; color: #fff; padding: 12px 30px; border-radius: 30px; font-weight: 600; text-decoration: none; display: inline-flex; align-items: center; justify-content: center; line-height: 1; height: auto; min-width: 150px; border: none; box-shadow: none;">Subscribe</a>
            </div>
            {{/if}}
        {{/if}}

        {{!-- If JS isn't enabled, fallback to standard pagination --}}
        <noscript>
            <div class="gh-pagination">
                {{pagination}}
            </div>
        </noscript>
    </div>
</div>
